CTF Challenges plugin
======================

Drop this folder into `CTFd/plugins/` (or install via your deployment process). CTFd will call the `load(app)` function in `__init__.py` which registers the challenge routes.

After installing, visit `/challenge-4/` and `/challenge-5/` on your CTFd host to access the integrated challenges.
